# Connect at https://dashboard.render.com → New → Blueprint
# DATABASE_URL must be set manually in the Render dashboard after deploy
# (use a free Neon PostgreSQL: https://neon.tech)
services:
  - type: web
    name: circuit-api
    runtime: python
    rootDir: backend
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /health
    envVars:
      - key: DATABASE_URL
        sync: false
      - key: AUTH_REQUIRED
        value: "true"
      - key: CORS_ORIGINS
        value: https://sameeradsv.github.io
      - key: FRONTEND_URL
        value: https://sameeradsv.github.io/circuit
      - key: CORTEX_AUTH_URL
        sync: false
      - key: PYTHON_VERSION
        value: "3.12"
