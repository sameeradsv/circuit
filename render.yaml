# Connect at https://dashboard.render.com → New → Blueprint
# Free PostgreSQL expires after 90 days on free plan; recreate via Blueprint to reset.
databases:
  - name: circuit-db
    plan: free
    region: oregon

services:
  - type: web
    name: circuit-api
    runtime: python
    rootDir: backend
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /health
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: circuit-db
          property: connectionString
      - key: AUTH_REQUIRED
        value: "true"
      - key: CORS_ORIGINS
        value: https://sameeradsv.github.io,http://localhost:3000,http://127.0.0.1:3000
      - key: PYTHON_VERSION
        value: "3.12"
